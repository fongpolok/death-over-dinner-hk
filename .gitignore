# Python
backend/__pycache__/
*.pyc

# Real SMTP credentials -- see backend/.env.example
backend/.env

# Remotion build output/cache
intro-video/node_modules/
intro-video/out/

# Runtime logs
data/*.log

# Impeccable working state (heavy binary mocks, review screenshots, local overrides)
.impeccable/mocks/
.impeccable/review/
.impeccable/hook.cache.json
.impeccable/config.local.json
.impeccable/live/

# OS
.DS_Store

# Claude Code / Impeccable skill tooling (not part of the shipped site)
.claude/
.impeccable/questions/

